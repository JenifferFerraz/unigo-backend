# Variáveis para o usuário/grupo dentro do contêiner (para permissões)
# Geralmente 1000 é o UID/GID do seu usuário padrão em Linux/macOS.
# No Windows, pode ser diferente, mas 1000 costuma funcionar bem para Docker.
DK_UID=1000
DK_GID=1000

# Variáveis para o Banco de Dados (PostgreSQL)
DB_HOST=db
DB_USER=seuuser
DB_PASSWORD=suasenha # Altere para uma senha forte e segura!
DB_NAME=unigo_db
DB_PORT=5433 # Porta do host para acessar o DB (DBeaver/pgAdmin)
DB_PORT_INTERNAL=5432 # Porta interna do contêiner PostgreSQL

# Variáveis para o Backend (Node.js Express)
BACKEND_PORT=3002 # Porta do host para acessar sua aplicação backend (e porta interna do contêiner)

# Outras variáveis de ambiente que sua aplicação Node.js precisa
# (Estas devem ser lidas pela sua aplicação Node.js dentro do contêiner)
JWT_SECRET=sua_chave_secreta_jwt_aqui_muito_longa_e_aleatoria
SMTP_HOST=smtp.exemplo.com
SMTP_PORT=587
SMTP_USER=seu_email@exemplo.com
SMTP_PASS=sua_senha_smtp
SMTP_FROM=seu_email@exemplo.com
FRONTEND_URL=http://localhost:3001 # Ou a porta que seu frontend Docker está mapeado
